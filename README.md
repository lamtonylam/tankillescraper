# Tankille.fi Scraper and GraphQL API

### Quickstart

```bash
npm i
npm run dev
```

Or with Docker:

```bash
docker-compose up
```

Go to
<https://localhost:4000/graphql> to access the GraphQL playground.
